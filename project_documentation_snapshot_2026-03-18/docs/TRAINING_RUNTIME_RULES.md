# Training Runtime Rules

- Checkpoints must be saved outside git
- Final model states are archived externally
- Git contains NO runtime artifacts
- Before every long run:
  - git status must be clean
  - .gitignore verified
