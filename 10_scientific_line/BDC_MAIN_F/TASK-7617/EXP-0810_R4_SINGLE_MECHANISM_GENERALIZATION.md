# EXP-0810: R4 Single-Mechanism Generalization

## Status
- Date: 2026-03-19
- Status: ACTIVE SCIENTIFIC PACKAGE
- Depends on:
  - `docs/experiments/POST_SECOND_SIGNAL_GATE_DECISION.md`
  - `docs/project/BDC_SCIENTIFIC_PREEXPERIMENT_GATE_PROTOCOL.md`

## Purpose

This package opens the next bounded scientific gate selected after the post-second-signal decision.

Selected next gate:
- `single_mechanism_generalization`

The purpose is to test whether the same approved replayable memory mechanism survives broader pressure inside the same family, without adding a second mechanism.

## Scientific question

Can `bounded_working_memory_candidate` retain superiority under broader but still bounded sequence-memory pressure?

## Scope boundary

This package stays inside:
- the approved environment family `controlled_sequence_memory`,
- the approved single mechanism family `bounded_working_memory_candidate`.

This package does not authorize:
- second mechanism addition,
- assembly rhetoric,
- organism or cell claims,
- reopening environment choice.

## Required generalization pressure families

At minimum, the next implementation surface must consider:
1. longer sequence length
2. larger recall gap
3. larger alphabet size
4. combined pressure slice with bounded limits

## Core rule

The mechanism must generalize as the same mechanism.

It must not be silently replaced by:
- a wider memory architecture,
- a second state channel,
- adaptive learning,
- or a second mechanism layer.

## Decision gate

This generalization cycle must end with one of:
- `CONFIRM_SINGLE_MECHANISM_GENERALIZATION`
- `REMAIN_IN_R4_GENERALIZATION`

## What this cycle must prove

It must prove that the validated FIFO memory mechanism is not only a narrow winner on two bounded slices, but remains effective under broader pressure while preserving its original identity.

## What this cycle must not claim

It must not claim:
- assembly readiness,
- multi-mechanism readiness,
- organism readiness,
- or cell readiness.

## Immediate next step

The correct next step after this package is:
1. define the bounded generalization pressure matrix,
2. implement deterministic generalization artifacts,
3. run the measured generalization gate.
