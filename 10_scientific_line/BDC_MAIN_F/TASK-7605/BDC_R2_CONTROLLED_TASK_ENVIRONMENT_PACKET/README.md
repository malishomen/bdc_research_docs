# BDC R2 Controlled Task Environment Packet

This folder is the bounded BDC-facing packet surface for the next scientific reboot phase after `R1`.

Its purpose is narrow:
- frame the `R2` environment-selection question,
- preserve the evidence-state surface after `R1 PASS_TO_R2`,
- keep `BDC Designer` available as the first analytical gate before direct scientific execution.

This packet does not claim that `R2` is already approved.
It exists to preserve the next-gate structure.
